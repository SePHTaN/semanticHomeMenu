uid: wp_fastChoice_setter
tags: []
props:
  parameters:
    - context: item
      description: Gruppe welche die Schedule-Timeitems enthält
      label: Heizungs Gruppe
      name: group
      required: false
      type: TEXT
    - context: item
      description: Item für Beginn- oder Ende-Datum je nachdem was bearbeitet wird
      label: Item Beginn / Ende Datum
      name: dateItem
      required: false
      type: TEXT
    - context: item
      description: Welches Holiday-Programm das bearbeitet wird holiday oder holidayAtHome
      label: holiday oder holidayAtHome Datum in Bearbeitung
      name: holidayType
      required: false
      type: TEXT
    - context: item
      description: Welches Datums-Item das bearbeitet wird Start oder Ende
      label: Start oder Ende- Datum in Bearbeitung
      name: dateType
      required: false
      type: TEXT
    - context: item
      description: String-Item mit dem Titel des Popovers
      label: Titel des Popovers
      name: title
      required: false
      type: TEXT
    - context: item
      description: Das Popover das geschlossen werden muss
      label: Name des aktuellen Popovers
      name: popup
      required: false
      type: TEXT
    - context: Text
      label: ColorScheme
      name: setterColorScheme
      required: true
      type: TEXT
  parameterGroups: []
timestamp: Feb 16, 2026, 1:26:36 PM
component: f7-block
config:
  class:
    - no-margin-vertical
    - no-padding-horizontal
  label: Datum bearbeiten
  style:
    --rgb-colorScheme: '=props.setterColorScheme =="red" ? "255,0,0" :
      props.setterColorScheme =="cyan" ? "3,255,255" : "255,153,0"'
  stylesheet: |
    div.item-content {
      padding: 8px
    }
  tooltip: ""
slots:
  default:
    - component: oh-context
      config:
        variables:
          datumsItem: =props.group+props.holidayType+props.dateType
          datumsStartItem: =props.group+props.holidayType+'start'
          datumsEndeItem: =props.group+props.holidayType+'end'
      slots:
        default:
          - component: f7-row
            config:
              class:
                - display-flex
                - no-margin
                - no-padding
                - justify-content-center
                - align-items-center
                - text-align-center
              style:
                background-color: =(!props.setterColorScheme)?orange:props.setterColorScheme
            slots:
              default:
                - component: f7-col
                  config:
                    style:
                      flex-grow: 1
                      padding-left: 60px
                  slots:
                    default:
                      - component: Label
                        config:
                          style:
                            font-size: 14px
                            font-weight: 700
                          text: =props.title+' Datum Ferien-Programm'
                - component: f7-col
                  config:
                    style:
                      width: auto
                  slots:
                    default:
                      - component: oh-button
                        config:
                          class:
                            - display-flex
                            - float-right
                          popoverClose: ="."+props.popup
                          style:
                            font-size: 14px
                            font-weight: 600
                            height: 26px
                          text: Close
          - component: f7-block
            config:
              class:
                - margin-vertical-half
                - padding-horizontal-half
              style:
                display: grid
                grid-template-columns: 1fr 3fr 2fr
                row-gap: 0px
            slots:
              default:
                - component: Label
                  config:
                    style:
                      font-size: 120%
                      font-weight: 700
                      grid-column: span 3
                      height: 30px
                      line-height: 26px
                      text-align: center
                    text: =props.title+"-Datum"
                - component: Label
                  config:
                    style:
                      text-align: center
                    text: Tag
                - component: Label
                  config:
                    style:
                      text-align: center
                    text: Monat
                - component: Label
                  config:
                    style:
                      text-align: center
                    text: Jahr
                - component: oh-button
                  config:
                    action: command
                    actionCommand: =dayjs(@(vars.datumsItem)).add(1,'day').format('YYYY-MM-DD')
                    actionItem: =vars.datumsItem
                    style:
                      --f7-button-text-color: var(--f7-text-color)
                      font-family: u2400
                      font-size: 110%
                  slots:
                    default:
                      - component: oh-link
                        config:
                          action: command
                          actionCommand: "=(props.dateType=='start')?
                            (dayjs(@(vars.datumsItem)).add(1,'day')>dayjs(@(var\
                            s.datumsEndeItem)))?
                            ((dayjs(@(vars.datumsItem)).diff(dayjs(@(vars.datum\
                            sEndeItem))))>1)?
                            dayjs(@(vars.datumsItem)).add(1,'day').format('YYYY\
                            -MM-DD'):
                            dayjs(@(vars.datumsEndeItem)).add(1,'day').format('\
                            YYYY-MM-DD'):
                            dayjs(@(vars.datumsEndeItem)).format('YYYY-MM-DD'):
                            dayjs(@(vars.datumsEndeItem)).format('YYYY-MM-DD')"
                          actionItem: =vars.datumsEndeItem
                          style:
                            --f7-button-text-color: var(--f7-text-color)
                            font-family: u2400
                            font-size: 110%
                            color: black
                            width: 100%
                          text: ▲
                - component: oh-button
                  config:
                    action: command
                    actionCommand: =dayjs(@(vars.datumsItem)).add(1,'month').format('YYYY-MM-DD')
                    actionItem: =vars.datumsItem
                    style:
                      grid-column-start: 2
                      --f7-button-text-color: var(--f7-text-color)
                      font-family: u2400
                      font-size: 110%
                  slots:
                    default:
                      - component: oh-link
                        config:
                          action: command
                          actionCommand: "=(props.dateType=='start')?
                            (dayjs(@(vars.datumsItem)).add(1,'month')>dayjs(@(v\
                            ars.datumsEndeItem)))?
                            (Math.abs(dayjs(@(vars.datumsItem)).add(1,'month').\
                            diff(dayjs(@(vars.datumsEndeItem)),'day'))>1)?
                            dayjs(@(vars.datumsItem)).add(1,'month').format('YY\
                            YY-MM-DD'):
                            dayjs(@(vars.datumsEndeItem)).add(1,'month').format\
                            ('YYYY-MM-DD'):
                            dayjs(@(vars.datumsEndeItem)).format('YYYY-MM-DD'):
                            dayjs(@(vars.datumsEndeItem)).format('YYYY-MM-DD')"
                          actionItem: =vars.datumsEndeItem
                          style:
                            --f7-button-text-color: var(--f7-text-color)
                            font-family: u2400
                            font-size: 110%
                            color: black
                            width: 100%
                          text: ▲
                - component: oh-button
                  config:
                    action: command
                    actionCommand: =dayjs(@(vars.datumsItem)).add(1,'year').format('YYYY-MM-DD')
                    actionItem: =vars.datumsItem
                    style:
                      --f7-button-text-color: var(--f7-text-color)
                      font-family: u2400
                      font-size: 110%
                  slots:
                    default:
                      - component: oh-link
                        config:
                          action: command
                          actionCommand: "=(props.dateType=='start')?
                            dayjs(@(vars.datumsItem)).add(1,'year')>(dayjs(@(va\
                            rs.datumsEndeItem)))?
                            (dayjs(@(vars.datumsItem)).add(1,'year').diff(dayjs\
                            (@(vars.datumsEndeItem)),'day')>1)?
                            dayjs(@(vars.datumsItem)).add(1,'year').format('YYY\
                            Y-MM-DD'):
                            dayjs(@(vars.datumsEndeItem)).add(1,'year').format(\
                            'YYYY-MM-DD'):
                            dayjs(@(vars.datumsEndeItem)).format('YYYY-MM-DD'):
                            dayjs(@(vars.datumsEndeItem)).format('YYYY-MM-DD')"
                          actionItem: =vars.datumsEndeItem
                          style:
                            --f7-button-text-color: var(--f7-text-color)
                            font-family: u2400
                            font-size: 110%
                            color: black
                            width: 100%
                          text: ▲
                - component: oh-label-card
                  config:
                    label: =dayjs(@(vars.datumsItem)).format('DD')
                    style:
                      fontSize: 12px
                      margin: 0px 5px
                      white-space: nowrap
                      width: auto
                - component: oh-label-card
                  config:
                    label: =dayjs(@(vars.datumsItem)).format('MMMM')
                    style:
                      font-size: 12px !important
                      margin: 0px 5px
                      white-space: nowrap
                - component: oh-label-card
                  config:
                    label: =dayjs(@(vars.datumsItem)).format('YYYY')
                    style:
                      font-size: 20px
                      margin: 0px 5px
                      white-space: nowrap
                - component: oh-button
                  config:
                    action: command
                    actionCommand: "=(dayjs(@(vars.datumsItem)).add(-1,'day')<dayjs())?
                      dayjs(@(vars.datumsItem)).format('YYYY-MM-DD'):
                      dayjs(@(vars.datumsItem)).add(-1,'day').format('YYYY-MM-D\
                      D')"
                    actionItem: =vars.datumsItem
                    style:
                      --f7-button-text-color: var(--f7-text-color)
                      font-family: u2400
                      font-size: 110%
                  slots:
                    default:
                      - component: oh-link
                        config:
                          action: command
                          actionCommand: "=(props.dateType=='end')?
                            (dayjs(@(vars.datumsItem)).add(-1,'day')<(dayjs(@(v\
                            ars.datumsStartItem))))?
                            ((dayjs(@(vars.datumsItem)).diff(dayjs(@(vars.datum\
                            sStartItem)),'day'))>1)?
                            dayjs(@(vars.datumsItem)).add(-1,'day').format('YYY\
                            Y-MM-DD'):
                            dayjs(@(vars.datumsStartItem)).add(-1,'day').format\
                            ('YYYY-MM-DD'):
                            dayjs(@(vars.datumsStartItem)).format('YYYY-MM-DD'):
                            dayjs(@(vars.datumsStartItem)).format('YYYY-MM-DD')"
                          actionItem: =vars.datumsStartItem
                          style:
                            display: flex
                            flex-grow: 1 1 auto
                            --f7-button-text-color: var(--f7-text-color)
                            font-family: u2400
                            font-size: 110%
                            color: black
                            width: 100%
                          text: ▼
                - component: oh-button
                  config:
                    action: command
                    actionCommand: "=(dayjs(@(vars.datumsItem)).add(-1,'month')<dayjs())?
                      dayjs(@(vars.datumsItem)).format('YYYY-MM-DD'):
                      dayjs(@(vars.datumsItem)).add(-1,'month').format('YYYY-MM\
                      -DD')"
                    actionItem: =vars.datumsItem
                    style:
                      --f7-button-text-color: var(--f7-text-color)
                      font-family: u2400
                      font-size: 110%
                  slots:
                    default:
                      - component: oh-link
                        config:
                          action: command
                          actionCommand: "=(props.dateType=='end')?
                            (dayjs(@(vars.datumsItem)).add(-1,'month').isBefore\
                            (dayjs(@(vars.datumsStartItem))))?
                            ((dayjs(@(vars.datumsItem)).diff(dayjs(@(vars.datum\
                            sStartItem)),'day'))>1)?
                            dayjs(@(vars.datumsItem)).add(-1,'month').format('Y\
                            YYY-MM-DD'):
                            dayjs(@(vars.datumsStartItem)).add(-1,'month').form\
                            at('YYYY-MM-DD'):
                            dayjs(@(vars.datumsStartItem)).format('YYYY-MM-DD'):
                            dayjs(@(vars.datumsStartItem)).format('YYYY-MM-DD')"
                          actionItem: =vars.datumsStartItem
                          style:
                            grid-column-start: 2
                            --f7-button-text-color: var(--f7-text-color)
                            font-family: u2400
                            font-size: 110%
                            color: black
                            width: 100%
                          text: ▼
                - component: oh-button
                  config:
                    action: command
                    actionCommand: "=(dayjs(@(vars.datumsItem)).add(-1,'year')<dayjs())?
                      dayjs(@(vars.datumsItem)).format('YYYY-MM-DD'):
                      dayjs(@(vars.datumsItem)).add(-1,'year').format('YYYY-MM-\
                      DD')"
                    actionItem: =vars.datumsItem
                    style:
                      --f7-button-text-color: var(--f7-text-color)
                      font-family: u2400
                      font-size: 110%
                  slots:
                    default:
                      - component: oh-link
                        config:
                          action: command
                          actionCommand: "=(props.dateType=='end')?
                            ((dayjs(@(vars.datumsItem)).add(-1,'year').isBefore\
                            (@(vars.datumsStartItem))))?
                            ((dayjs(@(vars.datumsItem)).diff(dayjs(@(vars.datum\
                            sStartItem)),'day'))>1)?
                            dayjs(@(vars.datumsItem)).add(-1,'year').format('YY\
                            YY-MM-DD'):
                            dayjs(@(vars.datumsStartItem)).add(-1,'year').forma\
                            t('YYYY-MM-DD'):
                            dayjs(@(vars.datumsStartItem)).format('YYYY-MM-DD'):
                            dayjs(@(vars.datumsStartItem)).format('YYYY-MM-DD')"
                          actionItem: =vars.datumsStartItem
                          style:
                            --f7-button-text-color: var(--f7-text-color)
                            font-family: u2400
                            font-size: 110%
                            color: black
                            width: 100%
                          text: ▼
